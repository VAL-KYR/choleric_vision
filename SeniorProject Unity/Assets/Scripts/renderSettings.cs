using UnityEngine;
using System.Collections;

public class renderSettings : MonoBehaviour {

	// Use this for initialization
	void Start () {
		RenderSettings.fogColor = new Color(0.1F, 0.1F, 0.1F, 0.1F);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
